spring:
  application:
   name: dentravak
  cloud:
    consul:
      host: 192.0.2.5

#      discovery:
#        instanceId: ${spring.application.name}:${random.value}
#        healthCheckPath: /health-check
#        healthCheckInterval: 20s
